import { makeStyles } from "@material-ui/core";

const ClientsListStyle = makeStyles({
    client: {
        width: "80%",
        margin: "auto"
    },
    marginClient: {
        margin: " 3% auto"
    }
});

export default ClientsListStyle;
